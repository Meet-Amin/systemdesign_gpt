# SystemDesign-GPT

This is a Streamlit app that converts real project implementation tasks into structured architecture output. It uses an AI assistant (powered by OpenAI's `gpt-4o-mini`) to generate a full design document, including an architecture diagram.

## Features

- **Task-first workflow:** Paste an implementation task and generate architecture directly.
- **Industry-grade output:** Includes API contracts, data model entities, sequence flows, consistency decisions, reliability, security, observability/SLOs, deployment, DR, cost, testing, and ops runbook.
- **Quality scoring engine:** Each design is auto-scored across reliability, security, observability, delivery, and completeness.
- **Architecture alternatives:** Generates latency-first, cost-first, and delivery-first options with a decision matrix and recommendation.
- **Run metrics:** Tracks latency, token usage, and estimated model cost for each generation.
- **Architecture diagrams:** It generates a Mermaid diagram to visualize the architecture. If the AI messes up the diagram, the app tries to build a fallback from the components list.
- **Markdown export:** You can download the whole thing to a Markdown file when you're done.
- **Simple and fast:** It's a single-page Streamlit app that uses `gpt-4o-mini` to keep things quick and cheap.

## Structure
```
systemdesign_gpt/
  app.py
  api.py
  core/
    prompts.py
    generator.py
    schemas.py
    diagram.py
  .env.example
  requirements.txt
  README.md
```

## Requirements
- Python 3.10+
- [OpenAI Python SDK](https://github.com/openai/openai-python)
- Streamlit UI toolkit
- Pydantic for structured output enforcement

## Setup
1. Copy the repo and create a virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   ```
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Set your OpenAI key (never hardcode this value), either as an env var or in a `.env` file:
   ```bash
   export OPENAI_API_KEY="<your-key>"
   ```
   or
   ```bash
   echo 'OPENAI_API_KEY="<your-key>"' > .env
   ```

## Usage
1. Run the Streamlit app:
   ```bash
   streamlit run app.py
   ```
2. Run the FastAPI backend (optional API mode):
   ```bash
   uvicorn api:app --reload --port 8000
   ```
   - Portfolio endpoint: `POST /design-package-from-task` for full package output (design + score + alternatives + metrics).
3. The Streamlit interface starts with an example implementation task. Replace it with your project requirement.
4. Click **Generate architecture** for direct task-to-architecture output.
5. Download the Markdown report via the **Export to Markdown** button.

## Notes
- The app focuses on direct project-task architecture generation.
- The Mermaid diagram respects any valid flowchart generated by the LLM but also builds one from the component graph when missing.
- Error alerts surface if the OpenAI key is missing or the API returns malformed JSON.

## Example Workflow

**1. Input:**
> Implement real-time notifications for a project management SaaS app

**2. Output:**
A production-ready architecture package with implementation details, quality score, decision matrix, run metrics, and a visual architecture diagram.
